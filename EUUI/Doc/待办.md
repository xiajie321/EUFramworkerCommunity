# EUUI Editor 功能实现待办清单

> 创建时间：2026-02-13  
> 目标：基于Doc目录下的参考代码，逐步实现完整的EUUI编辑器工具链

---

## 📊 功能模块概览

### 模块A：UIEditor - UI面板创建与导出系统
核心职责：UI场景创建、节点绑定、代码生成、Prefab导出

### 模块B：UISpriteEditor - 图集管理系统
核心职责：图集自动生成、图片类型转换

### 模块C：UIKitHotbox - Scene视图快捷面板（可选）
核心职责：提供按住Space键的快捷操作面板

---

## 🎯 阶段1：基础设施搭建

### ✅ 已完成项
- [x] `UIEditorConstants.cs` - 路径常量配置
- [x] `UIPanel.Generated.sbn` - Scriban代码生成模板
- [x] `UINodeBind.cs` - 节点绑定组件
- [x] `UIPanelDescription.cs` - UI面板元数据组件
- [x] `UIEnum.cs` - UI相关枚举定义

### 📋 待实现功能

#### 步骤1.1：创建编辑器菜单结构
**目标**：建立菜单入口，验证编辑器环境

- [ ] 创建 `Editor/UIEditorMenu.cs`
- [ ] 添加测试菜单项 `Framework/UIKit/Test`
- [ ] 验证 `UniLogger` 可用性
- [ ] 验证 `Scriban` 包是否正确引用
- [ ] 测试菜单项是否正常显示

**验收标准**：
- Unity菜单栏显示 `Framework/UIKit/Test`
- 点击后控制台输出测试日志

---

#### 步骤1.2：实现Hierarchy视觉反馈
**目标**：在Hierarchy面板显示已绑定节点的标识

- [ ] 在 `UIEditorMenu.cs` 或单独文件中实现
- [ ] 静态构造函数中注册 `EditorApplication.hierarchyWindowItemOnGUI`
- [ ] 检测GameObject上的 `UINodeBind` 组件
- [ ] 绘制蓝色 `[ComponentType]` 标签（右对齐）

**参考代码位置**：`Doc/UIEditor.cs.txt` 第27-52行

**验收标准**：
- 添加UINodeBind组件后，Hierarchy右侧显示蓝色标签
- 标签内容为组件类型（如`[Button]`、`[Image]`）

---

#### 步骤1.3：实现节点绑定功能
**目标**：快捷键批量添加UINodeBind组件

- [ ] 添加菜单项 `[MenuItem("Framework/UIKit/Bind UI Node &b")]`
- [ ] 实现 `AddBindComponent()` 方法
- [ ] 遍历 `Selection.gameObjects`
- [ ] 使用 `Undo.AddComponent<UINodeBind>()` 添加组件（支持撤销）
- [ ] 避免重复添加（检查是否已存在）

**参考代码位置**：`Doc/UIEditor.cs.txt` 第134-144行

**验收标准**：
- 选中多个GameObject
- 按 `Alt+B` 快捷键
- 所有选中对象添加UINodeBind组件（已有的不重复添加）
- 支持Ctrl+Z撤销

---

## 🎨 阶段2：UI创建流程

#### 步骤2.1：实现参数输入窗口
**目标**：创建UI面板时的参数配置窗口

- [ ] 创建 `Editor/UIPanelNameInputWindow.cs`
- [ ] 继承 `EditorWindow`
- [ ] 实现面板名称输入框
- [ ] 使用 `SerializedObject` 可视化编辑 `UIPanelDescription` 字段
- [ ] 实现窗口居中方法 `CenterOnMainWin()`
- [ ] 点击"确认创建"时自动添加"Wnd"前缀（如果没有）
- [ ] 回调函数传递 `panelName` 和 `UIPanelDescription` 实例

**参考代码位置**：
- `Doc/UIEditor.cs.txt` 第425-524行（窗口主体）
- 第514-524行（窗口居中扩展方法）

**技术要点**：
- 使用 `HideFlags.DontSave` 创建临时GameObject避免锁定
- 使用 `SerializedProperty` 自动绘制字段
- `EditorUtility.CopySerialized()` 实现数据拷贝

**验收标准**：
- 调用 `UIPanelNameInputWindow.ShowWindow()` 弹出窗口
- 窗口在主窗口居中显示
- 可编辑PackageName、PackageType、PanelType、Namespace
- 输入"Test"自动补全为"WndTest"

---

#### 步骤2.2：实现UI模板创建
**目标**：生成标准UI场景结构

- [ ] 实现 `ExecuteCreateUITemplate(string panelName, UIPanelDescription template)` 方法
- [ ] 创建新场景 `EditorSceneManager.NewScene()`
- [ ] 创建根节点并挂载 `UIPanelDescription`（使用CopySerialized拷贝参数）
- [ ] 创建环境节点：
  - [ ] Main Camera（Camera组件，SolidColor黑色背景）
  - [ ] Directional Light（Light组件）
- [ ] 创建Canvas结构：
  - [ ] Canvas（Canvas + CanvasScaler + GraphicRaycaster）
  - [ ] NotExportRoot_Bottom（子层）
  - [ ] ExportRoot（子层，UI元素挂载点）
  - [ ] NotExportRoot_Top（子层）
- [ ] 创建EventSystem（EventSystem + StandaloneInputModule）
- [ ] 保存场景到 `UISceneSavePath/{PackageName}/{panelName}.unity`
- [ ] 选中根节点

**参考代码位置**：`Doc/UIEditor.cs.txt` 第61-126行

**技术要点**：
- RectTransform铺满：anchorMin=(0,0), anchorMax=(1,1), sizeDelta=(0,0)
- 使用 `UIEditorConstants.EnsureDirectory()` 确保目录存在

**验收标准**：
- 场景保存到 `Assets/Res/CreateUIScenes/{PackageName}/WndTest.unity`
- Hierarchy结构完整且正确
- Canvas的三个子层RectTransform铺满
- UIPanelDescription数据正确保存

---

#### 步骤2.3：整合创建流程
**目标**：提供统一的UI创建入口

- [ ] 添加菜单项 `Framework/UIKit/Create UI Panel`
- [ ] 调用 `UIPanelNameInputWindow.ShowWindow()`
- [ ] 窗口确认后调用 `ExecuteCreateUITemplate()`

**验收标准**：
- 点击菜单 → 弹出窗口 → 输入参数 → 生成场景
- 完整流程无报错

---

## 🔧 阶段3：代码生成系统

#### 步骤3.1：实现工具辅助类
**目标**：创建UIEditorHelper工具类

- [ ] 创建 `Editor/UIEditorHelper.cs`
- [ ] 实现 `IsValidVariableName(string name, out string errorMessage)` 方法：
  - [ ] 检查是否为空
  - [ ] 检查是否为C#关键字（维护关键字HashSet）
  - [ ] 检查是否以数字开头
  - [ ] 正则验证：只包含字母、数字、下划线
- [ ] 实现 `GetRelativePath(Transform child, Transform root)` 方法
- [ ] 实现 `ToAssetPath(string fullPath)` 方法（绝对路径转Assets相对路径）

**参考代码位置**：`Doc/UIEditor.cs.txt` 第526-582行

**验收标准**：
- 正确识别非法变量名（关键字、数字开头、特殊字符）
- 路径转换正确

---

#### 步骤3.2：实现代码生成逻辑
**目标**：根据UINodeBind生成C#代码

- [ ] 实现 `StartExportProcess()` 方法：
  - [ ] 查找场景中的 `UIPanelDescription` 组件
  - [ ] 查找 `ExportRoot` GameObject
  - [ ] 收集ExportRoot下所有 `UINodeBind` 组件
  - [ ] 验证每个节点的变量名：
    - [ ] 调用 `IsValidVariableName()` 验证
    - [ ] 检查是否重名（使用HashSet）
    - [ ] 验证失败时显示错误对话框并返回
  - [ ] 调用 `GenerateCode()` 生成代码
  - [ ] 设置EditorPrefs标记（用于编译后回调）
  - [ ] 调用 `AssetDatabase.Refresh()` 触发编译

- [ ] 实现 `GenerateCode(string className, List<object> members, UIPanelDescription desc)` 方法：
  - [ ] 根据 `desc.PanelType` 选择基类：
    - `UIType.Popup` → `UIPopupPanelBase<T>`
    - `UIType.Bar` → `UIBarBase<T>`
    - 其他 → `UIPanelBase<T>`
  - [ ] 加载Scriban模板文件：`Assets/EUFramework/Extension/EUUI/Editor/Templates/UIPanel.Generated.sbn`
  - [ ] 生成 `.Generated.cs` 文件（字段绑定代码）：
    - 上下文：`{ is_gen: true, namespace_name, class_name, members }`
    - 保存路径：`UIBindScripts/{className}.Generated.cs`
    - **每次都覆盖**
  - [ ] 生成 `.cs` 文件（业务逻辑代码）：
    - 上下文：`{ is_gen: false, namespace_name, class_name, base_class, package_name }`
    - 保存路径：`UILogicScripts/{PackageName}/{className}.cs`
    - **仅当文件不存在时生成**
  - [ ] 确保目录存在
  - [ ] 使用UTF8编码写入文件

**参考代码位置**：
- `Doc/UIEditor.cs.txt` 第149-255行

**技术要点**：
- `Template.Parse()` 和 `template.Render()` 使用Scriban
- members格式：`{ name: "btnClose", type: "Button" }`

**验收标准**：
- 在ExportRoot下添加几个UINodeBind节点
- 执行导出流程
- 生成 `Assets/Scripts/Generated/UI/WndTest.Generated.cs`（字段定义）
- 生成 `Assets/Scripts/Game/UI/Test/WndTest.cs`（业务逻辑，仅首次）
- 代码格式正确，无语法错误

---

#### 步骤3.3：实现编译后自动绑定
**目标**：代码生成后自动完成字段赋值

- [ ] 实现 `OnScriptsReloaded()` 方法：
  - [ ] 添加 `[DidReloadScripts]` 特性
  - [ ] 检查 `EditorPrefs.GetBool(k_AutoBindKey)` 标记
  - [ ] 获取待处理的panelName
  - [ ] 调用 `PerformBinding(panelName)`
  - [ ] 清除EditorPrefs标记

- [ ] 实现 `PerformBinding(string panelName)` 方法：
  - [ ] 查找 `ExportRoot` GameObject
  - [ ] 获取 `UIPanelDescription` 确定命名空间
  - [ ] 使用反射获取生成的类型：`Assembly.Load("Assembly-CSharp").GetType(ns + "." + panelName)`
  - [ ] 检查类型是否存在（可能编译失败）
  - [ ] 挂载组件到ExportRoot（或获取已有组件）
  - [ ] 遍历所有 `UINodeBind` 节点：
    - [ ] 反射获取字段 `type.GetField(node.GetFinalMemberName())`
    - [ ] 获取目标组件 `node.GetComponent(node.GetFinalComponentType().ToString())`
    - [ ] 字段赋值 `field.SetValue(comp, targetComp)`
  - [ ] 调用 `FinalizePrefab()` 执行Prefab导出

**参考代码位置**：
- `Doc/UIEditor.cs.txt` 第259-345行

**EditorPrefs键名**：
```csharp
private const string k_AutoBindKey = "UIKit_AutoBind_Pending";
private const string k_PendingSceneKey = "UIKit_Pending_Scene";
```

**验收标准**：
- 代码生成后等待编译完成
- 自动在ExportRoot上挂载生成的组件
- Inspector中可见所有字段已正确赋值
- 无需手动拖拽

---

## 📦 阶段4：Prefab导出

#### 步骤4.1：实现Prefab保存
**目标**：将ExportRoot导出为干净的Prefab

- [ ] 实现 `FinalizePrefab(GameObject exportRoot, string panelName)` 方法：
  - [ ] 获取 `UIPanelDescription.PackageType`
  - [ ] 根据类型选择路径：
    - `UIPackageType.Builtin` → `UIPrefabSaveBuiltinPath`
    - `UIPackageType.Remote` → `UIPrefabSavePath`
  - [ ] 确保目录存在
  - [ ] 步骤1：`PrefabUtility.SaveAsPrefabAsset(exportRoot, prefabPath)` 保存原始对象
  - [ ] 步骤2：`PrefabUtility.LoadPrefabContents(prefabPath)` 加载副本
  - [ ] 步骤3：删除副本中所有 `UINodeBind` 组件（倒序遍历）
  - [ ] 步骤4：`PrefabUtility.SaveAsPrefabAsset(prefabContents, prefabPath)` 回写
  - [ ] 步骤5：`PrefabUtility.UnloadPrefabContents(prefabContents)` 释放副本
  - [ ] 输出成功日志

**参考代码位置**：`Doc/UIEditor.cs.txt` 第314-345行

**技术要点**：
- 必须使用LoadPrefabContents/UnloadPrefabContents配对
- 倒序遍历删除组件避免索引问题

**验收标准**：
- Prefab保存到正确路径
- 打开Prefab预制体，无UINodeBind组件残留
- 字段引用完整

---

#### 步骤4.2：实现资源清理功能
**目标**：删除废弃的Generated脚本和Prefab

- [ ] 实现 `CleanupOrphanedAssets()` 方法：
  - [ ] 扫描 `UISceneSavePath` 获取所有 `.unity` 场景文件（递归）
  - [ ] 提取场景文件名到 `HashSet<string> validPanelNames`
  - [ ] 清理Generated脚本：
    - [ ] 遍历 `UIBindScripts` 目录下的 `*.Generated.cs` 文件
    - [ ] 提取panelName（去除.Generated.cs后缀）
    - [ ] 如果不在validPanelNames中，使用 `AssetDatabase.DeleteAsset()` 删除
  - [ ] 清理Prefab：
    - [ ] 遍历 `UIPrefabSaveBuiltinPath` 和 `UIPrefabSavePath` 两个目录
    - [ ] 查找 `*.prefab` 文件
    - [ ] 如果不在validPanelNames中，删除
  - [ ] 调用 `AssetDatabase.Refresh()`
  - [ ] 显示对话框提示结果

**参考代码位置**：`Doc/UIEditor.cs.txt` 第350-418行

**验收标准**：
- 手动删除一个UI场景文件
- 执行清理功能
- 对应的.Generated.cs和.prefab被删除
- 控制台输出清理日志

---

#### 步骤4.3：添加导出菜单项
**目标**：提供菜单入口

- [ ] 添加菜单项 `Framework/UIKit/Export UI Panel`
- [ ] 调用 `StartExportProcess()`

**验收标准**：
- 完整流程测试：创建UI → 添加绑定 → 导出 → 生成Prefab
- 无报错，资源完整

---

## 🎨 阶段5：图集管理系统

#### 步骤5.1：实现图片类型转换
**目标**：批量将Texture转为Sprite类型

- [ ] 创建 `Editor/UISpriteEditor.cs`
- [ ] 实现 `ProcessTexturesInFolder(string folderPath)` 方法：
  - [ ] 使用 `AssetDatabase.FindAssets("t:Texture", new[] { folderPath })` 查找图片
  - [ ] 遍历每个GUID：
    - [ ] 转换为路径 `GUIDToAssetPath()`
    - [ ] 获取 `TextureImporter`
    - [ ] 检查是否需要修改（不是Sprite或启用了Mipmap）
    - [ ] 修改配置：
      - `textureType = TextureImporterType.Sprite`
      - `mipmapEnabled = false`
      - `alphaIsTransparency = true`
      - `wrapMode = TextureWrapMode.Clamp`
    - [ ] 调用 `SaveAndReimport()`
  - [ ] 输出转换数量日志

**参考代码位置**：`Doc/UISpriteEditor.cs.txt` 第137-156行

**验收标准**：
- 文件夹内的Texture自动转为Sprite
- Mipmap被禁用

---

#### 步骤5.2：实现图集参数配置
**目标**：设置SpriteAtlas标准参数

- [ ] 实现 `SetAtlasSettings(SpriteAtlas atlas)` 方法：
  - [ ] 配置Packing参数：
    - `blockOffset = 1`
    - `enableRotation = false`
    - `enableTightPacking = false`
    - `padding = 4`
  - [ ] 配置Texture参数：
    - `readable = false`
    - `generateMipMaps = false`
    - `sRGB = true`
    - `filterMode = FilterMode.Bilinear`

**参考代码位置**：`Doc/UISpriteEditor.cs.txt` 第158-177行

---

#### 步骤5.3：实现一键生成图集
**目标**：从选中文件夹生成SpriteAtlas

- [ ] 实现 `GetSelectedFolderPath()` 方法：
  - [ ] 遍历 `Selection.GetFiltered<Object>(SelectionMode.Assets)`
  - [ ] 检查是否为文件夹 `AssetDatabase.IsValidFolder()`
  - [ ] 如果是文件，获取其父目录
  - [ ] 返回第一个有效文件夹路径

- [ ] 实现 `GenerateAtlasFromFolder()` 方法：
  - [ ] 添加 `[MenuItem("Framework/UIKit/Sprite/一键生成图集 &g")]`
  - [ ] 获取选中文件夹路径
  - [ ] 智能识别保存目录：
    - 如果路径包含 `/Builtin/` → `UIAtlasBuiltinSavePath`
    - 否则 → `UIAtlasSavePath`
  - [ ] 获取文件夹名作为图集名
  - [ ] 调用 `ProcessTexturesInFolder()` 转换图片
  - [ ] 确保保存目录存在
  - [ ] 尝试加载现有图集 `AssetDatabase.LoadAssetAtPath<SpriteAtlas>()`
  - [ ] 如果不存在，创建新图集 `new SpriteAtlas()`
  - [ ] 调用 `SetAtlasSettings()` 设置参数
  - [ ] 关联文件夹：
    - [ ] `AssetDatabase.LoadAssetAtPath<Object>(folderPath)` 加载文件夹对象
    - [ ] `SpriteAtlasExtensions.Add(atlas, new[] { folderObj })`
  - [ ] 保存资源：
    - 新图集：`AssetDatabase.CreateAsset(atlas, atlasPath)`
    - 现有图集：已自动序列化
  - [ ] 调用 `AssetDatabase.SaveAssets()` 和 `Refresh()`
  - [ ] 选中生成的图集
  - [ ] 显示成功对话框

**参考代码位置**：
- `Doc/UISpriteEditor.cs.txt` 第28-135行

**验收标准**：
- 选中包含图片的文件夹
- 按 `Alt+G` 或点击菜单
- 图集生成到正确路径（Builtin或Remote）
- 图集正确关联文件夹
- 可在Inspector中点击"Pack Preview"预览

---

#### 步骤5.4：添加测试菜单（可选）
**目标**：验证UISpriteEditor初始化

- [ ] 添加 `[MenuItem("Framework/UIKit/Debug/Test Script Life")]`
- [ ] 输出日志并显示对话框
- [ ] 静态构造函数输出初始化日志

**验收标准**：
- 点击菜单显示"脚本运行正常"对话框

---

## 🎯 阶段6：Hotbox快捷面板（可选）

> 注意：此功能为可选增强功能，可在基础功能完成后实现

#### 步骤6.1：实现Hotbox显示系统
**目标**：按住Space键显示工具面板

- [ ] 创建 `Editor/UIKitHotbox.cs`
- [ ] 静态构造函数注册 `SceneView.duringSceneGui` 事件
- [ ] 实现 `OnSceneGUI(SceneView sceneView)` 方法：
  - [ ] 检测 `KeyDown` 事件且 `keyCode == KeyCode.Space`
  - [ ] 记录鼠标位置
  - [ ] 计算Hotbox矩形（320x260，居中于鼠标，边界限制）
  - [ ] 设置 `_isShowing = true`
  - [ ] 调用 `sceneView.Repaint()`
  - [ ] 检测 `KeyUp` 事件隐藏Hotbox
- [ ] 实现 `DrawHotbox()` 方法绘制UI

**参考代码位置**：`Doc/UIKitHotbox.cs.txt` 第12-75行

---

#### 步骤6.2：实现Hotbox UI绘制
**目标**：绘制分区布局和按钮

- [ ] 实现 `DrawBackground()` - 半透明背景和边框
- [ ] 实现 `DrawBorder()` - 绘制矩形边框
- [ ] 实现 `DrawCenteredLabel()` - 居中标签
- [ ] 实现 `DrawSectionHeader()` - 区域标题
- [ ] 实现 `DrawHotboxButton()` - 按钮样式

**布局结构**：
```
┌─────────────────────────────┐
│      UIKit Hotbox           │ (标题)
├─────────────────────────────┤
│        Panel 区域            │
│  [创建UI模板] [绑定节点] [导出Panel]│
│      [定位ExportRoot]        │
├──────────┬──────────────────┤
│ ListView │     其他         │
│ [生成VH] │ [清理废弃资源]    │
│ [生成Adapter] [刷新资源]     │
│ [清理VH] │ [返回GameBoot]   │
└──────────┴──────────────────┘
│   松开Space关闭              │ (底部提示)
└─────────────────────────────┘
```

**参考代码位置**：`Doc/UIKitHotbox.cs.txt` 第77-231行

---

#### 步骤6.3：实现功能集成
**目标**：按钮调用对应功能

- [ ] **Panel区按钮**：
  - [ ] 创建UI模板 → 调用 `UIPanelNameInputWindow.ShowWindow()`
  - [ ] 绑定节点 → 调用 `UIEditor.AddBindComponent()`
  - [ ] 导出Panel → 调用 `UIEditor.StartExportProcess()`
  - [ ] 定位ExportRoot → 调用 `LocateExportRoot()`

- [ ] **其他区按钮**：
  - [ ] 清理废弃资源 → 调用 `UIEditor.CleanupOrphanedAssets()`
  - [ ] 刷新资源 → 调用 `AssetDatabase.Refresh()`
  - [ ] 返回GameBoot → 调用 `ReturnToGameBoot()`

- [ ] **ListView区按钮（暂时注释或占位）**：
  - [ ] 如果ListViewEditor未实现，注释掉这部分按钮
  - [ ] 或创建空方法避免编译错误

**参考代码位置**：`Doc/UIKitHotbox.cs.txt` 第99-221行

---

#### 步骤6.4：实现ExportRoot定位功能
**目标**：快速定位并展开Hierarchy到ExportRoot

- [ ] 实现 `LocateExportRoot()` 方法：
  - [ ] 查找 `UIPanelDescription` 组件
  - [ ] 查找 `ExportRoot` GameObject
  - [ ] 设置 `Selection.activeGameObject`
  - [ ] 调用 `EditorGUIUtility.PingObject()`
  - [ ] 调用 `ExpandHierarchyToObject()` 展开父级

- [ ] 实现 `ExpandHierarchyToObject(GameObject target)` 方法：
  - [ ] 使用反射获取 `SceneHierarchyWindow` 类型
  - [ ] 获取窗口实例
  - [ ] 获取 `sceneHierarchy` 属性
  - [ ] 调用 `SetExpanded()` 方法展开所有父级

**参考代码位置**：`Doc/UIKitHotbox.cs.txt` 第308-372行

**验收标准**：
- 在Scene视图按住Space键显示Hotbox
- Hotbox居中于鼠标位置
- 所有按钮功能正常
- 松开Space键隐藏

---

## 📝 其他辅助功能

### 待实现的辅助功能

#### 返回GameBoot场景
- [ ] 实现 `ReturnToGameBoot()` 方法
- [ ] 检查当前场景是否有未保存修改
- [ ] 显示保存对话框（保存/不保存/取消）
- [ ] 打开GameBoot场景

**参考代码位置**：`Doc/UIKitHotbox.cs.txt` 第374-398行

**注意**：需根据实际项目修改GameBoot场景路径（第376行）

---

## ✅ 验收测试清单

### 完整功能测试流程

#### 测试1：UI创建流程
1. [ ] 点击菜单 `Framework/UIKit/Create UI Panel`
2. [ ] 输入面板名称"Test"，选择PackageName、PackageType等
3. [ ] 确认创建
4. [ ] 验证场景保存到正确路径
5. [ ] 验证Hierarchy结构完整
6. [ ] 验证UIPanelDescription数据正确

---

#### 测试2：节点绑定与导出流程
1. [ ] 在ExportRoot下创建UI元素（Button、Image等）
2. [ ] 选中多个元素，按 `Alt+B` 添加UINodeBind
3. [ ] 验证Hierarchy显示绑定标签
4. [ ] 在Inspector中配置ComponentType和MemberName
5. [ ] 点击菜单 `Framework/UIKit/Export UI Panel`
6. [ ] 等待编译完成
7. [ ] 验证代码生成：
   - [ ] `Assets/Scripts/Generated/UI/WndTest.Generated.cs` 存在
   - [ ] `Assets/Scripts/Game/UI/Test/WndTest.cs` 存在（仅首次）
8. [ ] 验证字段自动赋值：
   - [ ] ExportRoot上挂载了WndTest组件
   - [ ] Inspector中字段引用正确
9. [ ] 验证Prefab生成：
   - [ ] Prefab保存到正确路径（Builtin或Remote）
   - [ ] 打开Prefab无UINodeBind组件

---

#### 测试3：代码重新生成
1. [ ] 修改UINodeBind节点（添加/删除/重命名）
2. [ ] 再次执行导出
3. [ ] 验证 `.Generated.cs` 被覆盖
4. [ ] 验证 `.cs` 业务逻辑文件不被覆盖
5. [ ] 验证字段重新赋值

---

#### 测试4：错误处理
1. [ ] 测试非法变量名（数字开头、特殊字符）
2. [ ] 验证显示错误对话框
3. [ ] 测试重名变量
4. [ ] 验证显示冲突提示
5. [ ] 测试在非UI场景中导出
6. [ ] 验证显示错误提示

---

#### 测试5：图集生成流程
1. [ ] 创建文件夹 `Assets/Res/Remote/UI/TestImages`
2. [ ] 放入若干图片
3. [ ] 选中文件夹，按 `Alt+G`
4. [ ] 验证图片自动转为Sprite类型
5. [ ] 验证图集生成到 `Assets/Res/Remote/UI/Atlases/TestImages.spriteatlas`
6. [ ] 打开图集Inspector，点击"Pack Preview"预览
7. [ ] 测试Builtin路径识别

---

#### 测试6：资源清理
1. [ ] 手动删除一个UI场景文件
2. [ ] 点击菜单 `Framework/UIKit/Clean Orphaned Assets`（或通过Hotbox）
3. [ ] 确认对话框
4. [ ] 验证对应的.Generated.cs被删除
5. [ ] 验证对应的.prefab被删除
6. [ ] 验证控制台输出清理日志

---

#### 测试7：Hotbox功能（如已实现）
1. [ ] 打开任意UI场景
2. [ ] 在Scene视图按住 `Space` 键
3. [ ] 验证Hotbox显示在鼠标位置
4. [ ] 测试所有按钮功能
5. [ ] 测试"定位ExportRoot"功能
6. [ ] 松开Space键验证Hotbox隐藏

---

## 🔧 技术要点总结

### 关键技术清单

1. **EditorPrefs跨编译传递状态**
   - 保存待处理的场景名
   - `[DidReloadScripts]` 回调中读取

2. **反射操作**
   - 动态加载生成的类型
   - 展开Hierarchy（反射调用内部API）

3. **Scriban模板引擎**
   - `Template.Parse()` 解析模板
   - `template.Render(context)` 渲染
   - 上下文传递动态数据

4. **PrefabUtility操作**
   - `SaveAsPrefabAsset` 保存
   - `LoadPrefabContents/UnloadPrefabContents` 配对使用
   - 内存副本修改后回写

5. **路径处理**
   - `Application.dataPath` vs `Assets/` 相对路径
   - `Path.Combine()` 跨平台路径拼接
   - `AssetDatabase.IsValidFolder()` 验证文件夹

6. **SerializedObject/SerializedProperty**
   - 可视化编辑脚本字段
   - `EditorGUI.BeginChangeCheck/EndChangeCheck` 检测修改
   - `ApplyModifiedProperties()` 应用修改

7. **Undo系统**
   - `Undo.AddComponent()` 支持撤销
   - `Undo.RecordObject()` 记录对象修改

---

## 📚 参考资源

### 代码参考文件
- `Doc/UIEditor.cs.txt` - UI编辑器核心功能
- `Doc/UISpriteEditor.cs.txt` - 图集管理功能
- `Doc/UIKitHotbox.cs.txt` - Hotbox快捷面板

### Unity API文档
- EditorWindow
- SerializedObject/SerializedProperty
- PrefabUtility
- AssetDatabase
- TextureImporter
- SpriteAtlas

### 第三方依赖
- Scriban模板引擎：https://github.com/scriban/scriban

---

## 🎯 实施建议

### 开发顺序建议
1. **先实现阶段1-3**（创建+绑定+生成），打通核心流程
2. **再实现阶段4**（Prefab导出），完善导出功能
3. **然后实现阶段5**（图集管理），独立功能模块
4. **最后实现阶段6**（Hotbox），锦上添花

### 调试技巧
- 使用 `UniLogger.Log()` 输出关键步骤日志
- 使用 `Debug.Log()` 输出中间变量
- EditorPrefs调试：手动清除键值 `EditorPrefs.DeleteKey()`
- 代码生成失败时检查模板路径是否正确

### 常见问题
1. **编译后绑定失败**：检查命名空间、类名是否匹配
2. **Prefab引用丢失**：确保LoadPrefabContents/UnloadPrefabContents配对
3. **图集不显示**：确保图片是Sprite类型，文件夹正确关联
4. **Scriban模板报错**：检查语法，使用 `{{ }}` 包裹变量

---

## 📊 进度追踪

### 阶段完成情况

- [ ] 阶段1：基础设施搭建（预计3天）
- [ ] 阶段2：UI创建流程（预计3天）
- [ ] 阶段3：代码生成系统（预计5天）
- [ ] 阶段4：Prefab导出（预计2天）
- [ ] 阶段5：图集管理系统（预计2天）
- [ ] 阶段6：Hotbox快捷面板（预计2天，可选）

**预计总工时**：15-17天

---

## 🔄 更新记录

- **2026-02-13**：创建初始待办清单，基于Doc目录参考代码整理
